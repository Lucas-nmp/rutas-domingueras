{% extends 'base.html.twig' %}

{% block title %}Ruta{% endblock %}

{% block body %}
    <div class="administration">
        <h1>Ruta</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ ruta.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ ruta.name }}</td>
                </tr>
                <tr>
                    <th>Date_ruta</th>
                    <td>{{ ruta.dateRuta ? ruta.dateRuta|date('Y-m-d') : '' }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ ruta.description }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_ruta_index') }}">back to list</a>

        <a href="{{ path('app_ruta_edit', {'id': ruta.id}) }}">edit</a>

        {{ include('ruta/_delete_form.html.twig') }}
    </div>
{% endblock %}
