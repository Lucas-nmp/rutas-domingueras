{% extends 'base.html.twig' %}

{% block title %}Ruta{% endblock %}

{% block body %}
    <h1>Ruta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rutum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rutum.name }}</td>
            </tr>
            <tr>
                <th>Date_ruta</th>
                <td>{{ rutum.dateRuta ? rutum.dateRuta|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ rutum.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ruta_index') }}">back to list</a>

    <a href="{{ path('app_ruta_edit', {'id': rutum.id}) }}">edit</a>

    {{ include('ruta/_delete_form.html.twig') }}
{% endblock %}
